services:
  mongodb:
    image: mongo:7
    container_name: agentic-funding-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: agentic_funding

volumes:
  mongo_data:
